import styled from '@emotion/styled';

export const ChartHeader = () => {
  return <Header>서울특별시 자치구 시계열 차트</Header>;
};

const Header = styled.header`
  color: white;
  font-size: 40px;
  font-weight: 600;
  line-height: 1;
  margin: 0 0 40px 40px;
`;
